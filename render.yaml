services:
  - type: web
    name: monopoly-react-modern
    env: static
    buildCommand: npm ci && npm run build
    publishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
